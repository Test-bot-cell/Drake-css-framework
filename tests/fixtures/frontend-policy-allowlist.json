{
    "schemaVersion": 1,
    "maxWidthQueries": [
        {
            "file": "src/less/components/background.less",
            "condition": "(max-width: @breakpoint-xsmall-max)",
            "category": "bounded-responsive-api",
            "reason": "The class deliberately removes a selected background image below its named breakpoint; it does not define the base layout."
        },
        {
            "file": "src/less/components/background.less",
            "condition": "(max-width: @breakpoint-small-max)",
            "category": "bounded-responsive-api",
            "reason": "The class deliberately removes a selected background image below its named breakpoint; it does not define the base layout."
        },
        {
            "file": "src/less/components/background.less",
            "condition": "(max-width: @breakpoint-medium-max)",
            "category": "bounded-responsive-api",
            "reason": "The class deliberately removes a selected background image below its named breakpoint; it does not define the base layout."
        },
        {
            "file": "src/less/components/background.less",
            "condition": "(max-width: @breakpoint-large-max)",
            "category": "bounded-responsive-api",
            "reason": "The class deliberately removes a selected background image below its named breakpoint; it does not define the base layout."
        },
        {
            "file": "src/less/components/visibility.less",
            "condition": "(max-width: @breakpoint-xsmall-max)",
            "category": "bounded-responsive-api",
            "reason": "The visible-at-breakpoint utility encodes an explicit consumer choice and does not establish a component layout."
        },
        {
            "file": "src/less/components/visibility.less",
            "condition": "(max-width: @breakpoint-small-max)",
            "category": "bounded-responsive-api",
            "reason": "The visible-at-breakpoint utility encodes an explicit consumer choice and does not establish a component layout."
        },
        {
            "file": "src/less/components/visibility.less",
            "condition": "(max-width: @breakpoint-medium-max)",
            "category": "bounded-responsive-api",
            "reason": "The visible-at-breakpoint utility encodes an explicit consumer choice and does not establish a component layout."
        },
        {
            "file": "src/less/components/visibility.less",
            "condition": "(max-width: @breakpoint-large-max)",
            "category": "bounded-responsive-api",
            "reason": "The visible-at-breakpoint utility encodes an explicit consumer choice and does not establish a component layout."
        }
    ]
}
