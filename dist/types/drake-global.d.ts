// Généré par build/types.js : entrée globale pour les intégrations UMD (<script>).
import Drake from './drake';

declare global {
    interface Window {
        Drake: typeof Drake;
    }
}

export {};
